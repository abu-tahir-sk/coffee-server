import { Link } from "react-router-dom";

const SignIn = () => {
      return (
            <div>
                
                <Link to="/signUp">Sign In</Link>  
            </div>
      );
};

export default SignIn;